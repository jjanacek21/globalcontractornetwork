

# Rename "Storm Canvas Pro" → "iCanvas"

Two files need a simple text replacement:

1. **`src/components/crm/CRMSidebar.tsx`** (line 28) — Change sidebar nav title from "Storm Canvas Pro" to "iCanvas"
2. **`src/pages/crm/CRMStormCanvas.tsx`** (line 26) — Change page heading from "Storm Canvas Pro" to "iCanvas"

No routing, database, or structural changes needed — just two string replacements.

