

## Plan: Add Return to Dashboard Button on CRM Dashboard

**`src/pages/crm/CRMDashboard.tsx`**:
- Import `ReturnHomeButton` from `@/components/layout/ReturnHomeButton`
- Place it in the header section, top-right aligned next to the date badge, linking back to `/member/dashboard`

Single file, ~3 lines added.

